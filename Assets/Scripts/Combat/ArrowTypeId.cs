// Arrow type identifiers shared by LevelConfig budgets and future ArrowTypeConfig data (see SPEC.md 2.1).
// Soft launch starts with Normal only; the rest unlock later via campaign progress.
public enum ArrowTypeId
{
    Normal = 0,
    Magic = 1,
    Fire = 2,
    Ice = 3,
    Piercing = 4,
}
