using UnityEngine;

// Same combat body as the orc (hit zones, animator, HP). Only the rigged sprites differ.
public class EnemyGoblin : Enemy
{
    protected override void Awake()
    {
        base.Awake();
    }

    protected override void Start()
    {
        base.Start();
    }

    protected override void Update()
    {
        base.Update();
    }
}
